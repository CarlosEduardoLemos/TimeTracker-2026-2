import {useState} from 'react';import {IntegrationNotice} from '../components/IntegrationNotice';export function AuthPage({mode='login'}){const reg=mode==='cadastro';const [email,setEmail]=useState('');const [password,setPassword]=useState('');const valid=/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)&&password.length>=8;return <main className="grid min-h-screen place-items-center bg-page p-4"><section className="card w-full max-w-md"><a href="#/painel" className="text-xl font-extrabold">time<span className="text-brand">track</span></a><h1 className="mt-8 text-2xl font-extrabold">{reg?'Criar conta':'Entrar no Dashboard'}</h1><form className="mt-5 grid gap-4" onSubmit={e=>e.preventDefault()}><label className="text-sm font-semibold">E-mail<input className="form-field mt-1" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="text-sm font-semibold">Senha<input className="form-field mt-1" type="password" value={password} minLength="8" onChange={e=>setPassword(e.target.value)}/></label><button className="primary-button" disabled={!valid}>{reg?'Criar conta':'Entrar'}</button></form><div className="mt-5"><IntegrationNotice title="Autenticação não disponível">O frontend valida o formulário, mas o backend atual não expõe cadastro, login, sessão/token ou endpoint do código de associação de 6 dígitos. O envio permanece bloqueado para não criar autenticação fictícia.</IntegrationNotice></div><p className="mt-5 text-center text-sm muted"><a className="font-bold text-brand" href={reg?'#/login':'#/cadastro'}>{reg?'Já tenho conta':'Criar conta'}</a></p></section></main>}
